process {
    executor = 'awsbatch'
    queueSize = 100
    maxForks = 16          
    errorStrategy = 'retry'       
    maxRetries = 20  
    maxErrors = 100
}

process.resourceLabels = ['allen-batch-pipeline': 'aind-pophys-pipeline']
